<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Welcome <%= name %> </h1>
    <% if (gender == "male") { %>
     <h1>This is a male child</h1>
    <% }else{ %>
        <h1>This is a female child</h1>
    <% } %>
        
    <!-- <% for( let index = 0; index < allUsers.length; index++ ) { %>
    <h1><%= allUsers[index].firstName %></h1>
    <h1><%= allUsers[index].lastName %></h1>
    <h1><%= allUsers[index].email %></h1>
    <% } %> -->


    <% allUsers.map((user) => { %>
     <h1><%= user.firstName %></h1>
     <h1><%= user.lastName %></h1>
     <h1><%= user.email %></h1>
    <% }) %>
     
    
     
</body>
</html>